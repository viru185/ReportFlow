# Normalize line endings to LF in the repo (consistent across Windows/CI).
* text=auto eol=lf

# Binary assets — never touch line endings.
*.xlsx binary
*.xlsm binary
*.pdf binary
*.exe binary
*.png binary
*.ico binary
